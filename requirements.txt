# Core dependencies for the project
# If you're on Apple Silicon (M1/M2) see the Apple-specific notes below and
# install `tensorflow-macos` + `tensorflow-metal` instead of `tensorflow`.

tensorflow>=2.12.0
numpy>=1.24.0
matplotlib>=3.7.0
Pillow>=9.5.0

# Optional (only for Apple Silicon acceleration):
# tensorflow-macos>=2.12.0
# tensorflow-metal>=0.6.0
